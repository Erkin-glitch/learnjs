####hello
```
let array = [1,5,8,4]
function numbers(array) {
    for (let i = 0; i < array.length; i++){
       console.log(array[i]);
        
    }
    return "stop"
}
console.log(numbers(array))
```
